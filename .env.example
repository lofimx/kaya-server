# Kaya Server — production environment variables
# Copy to .env and fill in values, or set in Portainer stack environment.

RAILS_MASTER_KEY=<from config/master.key>
POSTGRES_USER=kaya
POSTGRES_PASSWORD=<generate a strong password>
